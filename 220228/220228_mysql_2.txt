DB 3가지 기능 (정의, 조작, 제어) 기능
DBMS의 3가지 기능
(정의어 DDL, 조작어 DML, 제어어 DCL)

1. DDL (정의어)
	1.1 DB 생성(create)
	1.2 DB 구조변경(alter)
	1.3 DB 삭제(drop)

2. DML (조작어)
	2.1 자료검색(select)	- 가장중요
	2.2 자료삽입(insert)
	2.3 자료삭제(delete)
	2.4 자료